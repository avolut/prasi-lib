export const prasi_user = window as unknown as {
  user: {
    id: string;
    username: string;
    name: string;
    fullname: string;
    m_role: {
      id: string;
      name: string;
    };
  };
  prasi_home: Record<string, string>;
};
